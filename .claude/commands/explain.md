---
description: Explain a security control in plain language, grounded in the registry
---

Follow `agents/knowledge/explaining-a-control.md`. Explain the control named below in
plain language, reading every fact from `controls/registry.yaml` and
`controls/ism-snapshot.json` at answer time — never from memory. Keep ISM detail out of
the answer unless it is asked for.

$ARGUMENTS
